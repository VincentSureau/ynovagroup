{% extends 'backend/backend_layout.html.twig' %}

{% block title %}Liste des partenaires{% endblock %}

{% block body %}
    <h1>Liste des partenaires</h1>

<div class="card p-4">
    {{ include('backend/partials/_flash.html.twig') }}
    <div class="d-flex mb-3">
        <a class="btn btn-success btn-sm" id="add-modal" href="{{ path('backend_partner_create') }}"><i class="fas fa-plus-square"></i> Ajouter un partenaire</a>
    </div>
    <table id="partnerTable" class="table table-hover table-striped w-100">
        <thead>
            <tr>
                <th>#</th>
                <th>Nom</th>
                <th>Image</th>
                <th>Lien</th>
                <th>Actif</th>
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>
</div>

{% endblock %}