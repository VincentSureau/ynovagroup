{% extends 'backend/backend_layout.html.twig' %}

{% block title %}Ajouter un partenaire{% endblock %}

{% block body %}
    <h1>Ajouter un partenaire</h1>
    <div class="card p-4">
    {{ include('backend/partials/_flash.html.twig') }}
    {% if partner.image is not null %}
        <img class="card-img-top" style="max-height: 400px" src="{{ vich_uploader_asset(partner, 'pictureFile') }}" alt="{{ partner.name|default('illustration du partneaire') }}" />
    {% endif %}
        {{ include('/backend/partner/_form.html.twig') }}    
    </div>
    <a class="link-green" href="{{ path('backend_partner_index') }}">retour à la liste des partenaires</a>
{% endblock %}
