{{ form_start(form) }}
    {{ form_widget(form) }}
    
    <div class="text-center mt-3">
        <button class="btn btn-success">{{ button_label|default('Envoyer') }} <i class="far fa-paper-plane"></i></button>
    </div>
{{ form_end(form) }}
